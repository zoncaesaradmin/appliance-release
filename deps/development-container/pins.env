# Pins for the combined tooling image published as dev-build.
DEBIAN_VARIANT=bookworm
GO_VERSION=1.26.0
NODE_VERSION=22.19.0
DEV_IMAGE_NAME=dev-build
# Default DEV_IMAGE_REPO when publishing to LAN (override via env).
# GHCR example: <user>/development-container
DEFAULT_DEV_IMAGE_REPO=development-container
