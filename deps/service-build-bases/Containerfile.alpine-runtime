ARG BASE_IMAGE=docker.io/library/alpine:3.24.1
FROM ${BASE_IMAGE}
# Packages used across CP / UI / hostagent / dns / argo wrappers (debug profile set).
RUN apk add --no-cache \
      bash ca-certificates tzdata \
      bind-tools curl iproute2 iputils procps vim
