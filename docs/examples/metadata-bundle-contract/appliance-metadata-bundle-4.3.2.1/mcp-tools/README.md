# MCP Tools

Reserved top-level metadata section for MCP-oriented tool descriptors and
policy.

The detailed payload contract is intentionally deferred. The important part for
the current Automation Runtime design is that this section exists parallel to
the other bundle sections and is not nested under a generic `workflows/`
directory.
